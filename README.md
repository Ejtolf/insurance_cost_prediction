# 📊 Medical Insurance Cost Prediction

## 📌 Описание проекта
Цель проекта — построить модель, которая предсказывает стоимость медицинской страховки на основе демографических и медицинских характеристик клиента.  

Задача решается как **регрессия**. Помимо построения моделей, проведён анализ факторов, влияющих на цену страховки, с выводами для бизнеса.

---

## 📂 Датасет
Источник: [Kaggle — Medical Cost Personal Dataset](https://www.kaggle.com/datasets/mirichoi0218/insurance)

**Признаки:**
- `age` — возраст  
- `sex` — пол  
- `bmi` — индекс массы тела  
- `children` — количество детей/иждивенцев  
- `smoker` — наличие привычки курения  
- `region` — регион проживания (northeast, northwest, southeast, southwest)  
- `charges` — стоимость страховки (**целевой признак**)  

---

## 🔎 Анализ данных (EDA)
- Курение — ключевой фактор, увеличивающий цену страховки в разы.  
- Возраст и индекс BMI также заметно влияют на стоимость.  
- Пол и количество детей практически не оказывают влияния.  
- Распределение `charges` сильно скошено вправо (наличие дорогих полисов).  

---

## ⚙️ Feature Engineering
- Кодирование категориальных признаков (`sex`, `smoker` → бинарные; `region` → one-hot).  
- Масштабирование числовых признаков (`age`, `bmi`, `children`).  
- Проверка дополнительных признаков (взаимодействие возраста и курения, BMI и курения).  
- Логарифмирование `charges` рассматривалось для снижения влияния выбросов.  

---

## 🤖 Модели
Рассматривались разные алгоритмы, но основной акцент сделан на **линейной регрессии** благодаря её интерпретируемости.  

### Метрики (Linear Regression):
- **MAE:** ~4177  
- **RMSE:** ~5956  
- **R²:** ~0.81  

---

## 📊 Интерпретация результатов
Анализ коэффициентов линейной регрессии (Ridge):
- **smoker** — главный драйвер роста цены.  
- **age** — второй по значимости фактор.  
- **bmi** — умеренное влияние.  
- **region**, **children**, **sex** — слабые или незначимые факторы.  

---

## 💡 Бизнес-выводы
- Модель объясняет ~81% вариации стоимости страховки и может использоваться для прогнозов.  
- Курение — ключевой риск-фактор: страховые компании могут использовать это для формирования тарифов.  
- Модель позволяет **объяснять клиентам** причины высокой цены (прозрачная интерпретация признаков).  
- Возможность разработки калькулятора (например, в Streamlit), чтобы пользователи могли оценить стоимость страховки онлайн.  

---
